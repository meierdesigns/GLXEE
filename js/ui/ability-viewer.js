"use strict";

// AbilityViewerUI is defined in ability-viewer/core.js and extended by the other files in ability-viewer/,
// which index.html loads before this file.
const abilityViewerUI = new AbilityViewerUI();
