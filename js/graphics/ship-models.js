"use strict";

// ShipModels is defined in ship-models/core.js and extended by the other files in ship-models/,
// which index.html loads before this file.
// Create global instance
const shipModels = new ShipModels();
