"use strict";

// EnemyConfigManager is defined in enemy-config/core.js and extended by the other files in enemy-config/,
// which index.html loads before this file.
const enemyConfigManager = new EnemyConfigManager();
