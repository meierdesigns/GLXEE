"use strict";

// LevelInfoManager is defined in level-info-manager/core.js and extended by the other files in level-info-manager/,
// which index.html loads before this file.
// Global instance
let levelInfoManager = new LevelInfoManager();
