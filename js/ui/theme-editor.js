"use strict";

// ThemeEditorUI is defined in theme-editor/core.js and extended by the other files in theme-editor/,
// which index.html loads before this file.
let themeEditorUI = new ThemeEditorUI();
