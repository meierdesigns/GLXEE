"use strict";

// EnemyViewerUI is defined in enemy-viewer/core.js and extended by the other files in enemy-viewer/,
// which index.html loads before this file.
const enemyViewerUI = new EnemyViewerUI();
