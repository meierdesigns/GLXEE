"use strict";

// EnemyManager is defined in enemies/core.js and extended by the other files in enemies/,
// which index.html loads before this file.
// Global enemy manager instance
const enemyManager = new EnemyManager();
