"use strict";

// ShipConfigManager is defined in ship-config/core.js and extended by the other files in ship-config/,
// which index.html loads before this file.
const shipConfigManager = new ShipConfigManager();
